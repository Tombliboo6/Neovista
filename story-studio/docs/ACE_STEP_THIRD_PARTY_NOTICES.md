# PRISM ACE-Step 1.5 第三方说明

本离线包包含 ACE-Step 1.5 源码、模型文件、CPython 3.12 运行时、PyTorch CUDA 运行时及其 Python 依赖。

- ACE-Step 1.5：MIT License；完整许可文本位于发布包根目录 `ACE-STEP-LICENSE.txt`，安装后也保留在 ACE-Step 根目录。
- CPython：Python Software Foundation License；完整许可文本随运行时保留在 `runtime/python/LICENSE.txt`。
- Python 与本机运行依赖：对应包的元数据和许可文件保留在 `.venv/Lib/site-packages` 的各 `*.dist-info` 目录内。
- 模型说明和随附许可信息保留在 `checkpoints` 各模型目录及 ACE-Step 自带文档中。

本说明用于定位随包许可文件，不替代各上游项目的完整许可文本。
