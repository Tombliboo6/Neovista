# NeoVista Project Context & AI Coding Guidelines

---

## ⚠️ 核心 API 与环境防篡改锁 (Read-Only Lock for Core APIs)

**最高级别只读纪律**：从现在起，对于本项目中已经配置好的核心 API 与环境变量，你只有『读取』权限，绝对没有『写入/修改』权限。

**具体保护名单：**

1. **绝对禁止修改、覆盖或重建 `.env` 文件**
   - 该文件包含已配置的 API 密钥和渠道信息
   - 任何对 `.env` 的修改都必须经过用户明确授权

2. **绝对禁止修改后端 API 调用逻辑代码**
   - 包括 DeepSeek 和 Google Imagen 3 Pro 的底层大模型调用逻辑
   - 除非用户明确下达『请重构 API 代码』的指令

**执行规则：**
- 如果在未来的开发中，你发现需要修改这些被保护的文件才能完成任务，你必须：
  1. 立即停止写入操作
  2. 在终端以纯文本形式打印修改建议
  3. 明确告知需要修改哪个受保护的文件
  4. 询问用户是否授权执行

---

# NeoVista Project Context & AI Coding Guidelines

## 1. 平台愿景与 UI 范式 (Vision & UI Blueprint)
- **平台定位**：NeoVista (neovista.cn) 是专为建筑、景观、规划设计师打造的 AIGC 智能分析图工作流平台。
- **UI 核心哲学**：摒弃传统 CAD/SU 的复杂面板，主打“一框式自然语言输入”与“右侧模块化工作流（Skills）封装”。
- **两大核心界面架构**：
  1. **首页控制台 (Home Dashboard)**：
     - **大搜中枢**：页面视觉中心为超大文本输入框，集成附件、地理定位、图像、3D模型上传入口。
     - **分类 Tab**：输入框下方提供专业场景切换（如 Design, Urban, Landscape 等）。
     - **项目管理**：下方为历史作品卡片网格（包含缩略图、名称、时间）及“新建项目”入口。
     - **极简顶导**：Logo、语言切换、会员升级、个人中心。
  2. **工作区/编辑界面 (Workspace - 三栏经典布局)**：
     - **左侧主导航 (Left Nav)**：垂直悬浮图标条（主页、新建、项目夹、个人中心）。
     - **中央大画布 (Center Canvas)**：占据绝大部分区域的空白 Fabric.js 操作区，用于可视化编辑。
     - **右侧技能面板 (Right Skills Panel)**：固定抽屉，封装专业工作流模板（NeoVista Skills）列表。
     - **底部 AI 对话框 (Agent Chat Input)**：锚定在右侧面板底部，带“Agent”开关和附件功能，支持持续对话细化图纸。

## 2. 核心业务领域与场景矩阵 (Core Domain Scenarios)
系统以后端 Agent 路由的方式，支持 9 大核心分析场景与 1 个展示大类：
1. **场地与语境分析**（肌理、区位、视线等）
2. **环境与物理性能**（日照、风环境、水系统等）
3. **概念与体块推演**（体块爆炸图、类型学矩阵等）
4. **功能与程序**（气泡图、堆叠轴测图等）
5. **流线与动线**（人车流线、街道剖面等）
6. **技术、构造与建造**（结构爆炸图、剖透视等）
7. **感官与用户体验**（视域分析、热舒适度等）
8. **植物分析**（生境剖面、植物配置等）
9. **专项分析**（人文热力图、碳排放等）
10. **成果展示**（手工推敲木模渲染、实景渲染等）

## 3. 核心算法：P0-P3 Prompt 优先级引擎 (Prompt Engineering)
后端必须严格遵循以下 Prompt 权重组装逻辑路由给闭源大模型 API：
- **【P0】文字内容与排版**：标题、数据、学术标注的准确性。
- **【P1】画面内容与细节**：主体空间结构、材质约束、特定元素（剪影/箭头）。
- **【P2】光影与氛围**：光照逻辑（AO/硬阴影），控制学术严谨或浪漫写实感。
- **【P3】构图与色彩**：视角（正交/轴测/顶视）和全局配色方案。

## 4. 技术栈架构 (Tech Stack)
- **前端**: React 18+ (Vite), Tailwind CSS, Fabric.js (中央画布), Zustand (状态管理), React Router (路由)。
- **后端**: Python 3.10+ + FastAPI。

## 5. 给 Codex 的工作流指令 (Workflow Rules for Codex)
- **组件化思维**：严格按照第 1 节的 UI 蓝图拆分组件，如 `HomeSearchBox`, `ProjectGrid`, `WorkspaceLayout`, `LeftNav`, `FabricCanvas`, `SkillsPanel`, `AgentInput`。
- **专业语料预设**：Mock 数据必须使用第 2 节的专业建筑/景观词汇。
- **增量开发**：严格按照我的单次指令执行，一次只做一个界面或组件。

## 6. Git 存档纪律 (Version Control Discipline)
**强制执行规则**：每次当你完成一个功能模块、或者修复了一个重要 Bug 且测试跑通后，你必须在终端输出明确的文字提醒：

```
【✅ 当前开发节点已闭环，建议你现在手动执行以下命令进行存档】
git add .
git commit -m "简明扼要的进度描述"
```

你只负责提醒，不自动执行 commit，让用户保留完全控制权。
